[pytest]
testpaths = tests
markers =
    integration: tests requiring a live API key (deselect with -m "not integration")
    ffmpeg: tests requiring FFmpeg installed (auto-skipped if not found)
addopts = -v --tb=short --no-header -q
